# Optional CDN URLs for live tournament data.
# When unset, the app reads committed snapshots from public/data/.
#
# Example (Cloudflare R2 public bucket or custom domain):
# VITE_APP_STATE_URL=https://data.example.com/app_state.json
#
# VITE_SCENARIO_APP_STATE_URL=
# VITE_BACKTEST_URL=
